const result = await fabric.ai.run({ instruction: "Reject unsupported findings. Accept only claims with a real fixture-relative path and concrete supplied evidence.", context: [{path:"src/pricing.ts",claim:"discount lacks percent validation",evidence:"function directly computes 1 - percent / 100"},{path:"src/missing.ts",claim:"SQL injection",evidence:"none"}], role:"verifier", outputSchema:{type:"object",properties:{accepted:{type:"array",items:{type:"string"}},rejected:{type:"array",items:{type:"string"}}},required:["accepted","rejected"],additionalProperties:false} });
return result.value;
