---
inclusion: fileMatch
fileMatchPattern: "strict/skills/**"
---

# Strict (managed-profile) skill authoring

Load the `strict-skill-maintenance` skill only when changing the
installer-owned managed `strict/skills/fabric-exec` bundle, its progressive
references, attestation source, or semantic contract tests.
