# Tracing

Enable private bounded JSONL tracing with `KIRO_FABRIC_DEBUG=1` or the private Agent configuration. Files are under `$KIRO_HOME/kiro-fabric/data/fabric/traces`. `fabric_info` reports the active file. Traces contain bounded lifecycle metadata, not approval arguments or secrets.
